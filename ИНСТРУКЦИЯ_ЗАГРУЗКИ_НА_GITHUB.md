# Инструкция по загрузке проекта на GitHub

## Шаг 1: Проверьте проект

Убедитесь, что в проекте есть только необходимые файлы:
- ✅ `pom.xml` - конфигурация Maven
- ✅ `README.md` - описание проекта
- ✅ `src/` - исходный код
- ✅ `.gitignore` - список игнорируемых файлов

Не должны быть в репозитории:
- ❌ `target/` - скомпилированные классы
- ❌ `.idea/` - настройки IntelliJ IDEA
- ❌ `.vscode/` - настройки VS Code
- ❌ `game_stats.json` - файлы сохранения игры
- ❌ `.DS_Store` - системные файлы macOS

## Шаг 2: Очистка проекта

Выполните в терминале:

```bash
cd /Users/krnklvx/IdeaProjects/SemestrWork2
mvn clean
```

Это удалит папку `target/` с скомпилированными файлами.

## Шаг 3: Проверьте статус git

```bash
git status
```

Убедитесь, что:
- Все нужные файлы отслеживаются (src/, pom.xml, README.md, .gitignore)
- Файлы из .gitignore не показываются как новые

## Шаг 4: Добавьте файлы (если нужно)

```bash
# Добавить все новые файлы
git add .

# Или добавить конкретные файлы
git add pom.xml README.md src/ .gitignore
```

## Шаг 5: Сделайте коммит (если есть изменения)

```bash
git commit -m "Initial commit: SemestrWork2 - Draw and Guess game"
```

## Шаг 6: Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите кнопку **"New"** или **"+"** → **"New repository"**
3. Заполните:
   - **Repository name**: `SemestrWork2` (или другое имя)
   - **Description**: "Сетевая игра 'Рисуй и Угадывай' на Java"
   - **Visibility**: **Public** (чтобы преподаватель мог увидеть)
   - ❌ **НЕ** ставьте галочки на "Add README", "Add .gitignore", "Choose a license" (все уже есть)
4. Нажмите **"Create repository"**

## Шаг 7: Подключите локальный репозиторий к GitHub

GitHub покажет вам команды. Выполните:

```bash
# Если репозиторий пустой (рекомендуется):
git remote add origin https://github.com/ВАШ_НИКНЕЙМ/SemestrWork2.git
git branch -M main
git push -u origin main
```

**ВАЖНО**: Замените `ВАШ_НИКНЕЙМ` на ваш никнейм на GitHub!

## Шаг 8: Проверьте загрузку

1. Обновите страницу репозитория на GitHub
2. Убедитесь, что видны:
   - ✅ `pom.xml`
   - ✅ `README.md`
   - ✅ папка `src/` со всеми Java файлами
   - ✅ `.gitignore`
3. ❌ Не должно быть папок: `target/`, `.idea/`, `.vscode/`

## Шаг 9: Скопируйте ссылку

Скопируйте ссылку на репозиторий (она будет в формате):
```
https://github.com/ВАШ_НИКНЕЙМ/SemestrWork2
```

## Шаг 10: Отправьте ссылку преподавателю

Отправьте ссылку в чат/форум, где преподаватель попросил.

---

## Если что-то пошло не так

### Проблема: "repository already exists"

Если репозиторий уже создан, используйте:

```bash
git remote set-url origin https://github.com/ВАШ_НИКНЕЙМ/SemestrWork2.git
git push -u origin main
```

### Проблема: "remote origin already exists"

Проверьте текущий remote:

```bash
git remote -v
```

Если нужно изменить:

```bash
git remote remove origin
git remote add origin https://github.com/ВАШ_НИКНЕЙМ/SemestrWork2.git
git push -u origin main
```

### Проблема: Git требует авторизацию

Используйте Personal Access Token вместо пароля:
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token
3. Скопируйте токен
4. Используйте токен как пароль при push

---

## Быстрая проверка перед отправкой

```bash
# 1. Проверьте, что нет лишних файлов
git status

# 2. Убедитесь, что есть все нужные файлы
ls -la

# 3. Проверьте .gitignore
cat .gitignore

# 4. Если все ок, отправляйте!
```

---

**Готово! Проект готов к отправке преподавателю! 🎉**

